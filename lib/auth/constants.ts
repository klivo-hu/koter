/** Shared between the Edge middleware and the Node server; must stay dependency-free. */
export const SESSION_COOKIE = 'koter_admin_session';
export const JWT_ISSUER = 'koter-gym';
export const JWT_AUDIENCE = 'koter-admin';
